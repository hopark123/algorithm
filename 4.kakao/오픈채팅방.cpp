#include <string>
#include <vector>
#include <iostream>
using namespace std;

vector<string> solution(vector<string> record) {
    vector<string> answer;
    return answer;
}


int main()
{
	ios::sync_with_stdio(false);
  vector<string> sol = solution("ABABABABABABABAB");
  for (size_t i = 0; i < sol.size(); ++i)
    cout << sol[i] << endl;
}
